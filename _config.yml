encoding: utf-8

baseurl: /sir

defaults:
  -
    scope:
      path: "_posts"
    values:
      layout: "post"
      premalink: /:categories/:title.html
  -
    scope:
      path: "_posts/system/os"
    values:
      categories: [system, os]

